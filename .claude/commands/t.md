---
name: t
description: Alias for /thread command
---

# Thread Command Alias

This is an alias for the `/thread` command. See `/help thread` for full documentation.

Behaves exactly like `/thread`.

**⚠️ 重要：执行前必须先检查 `mcp__thread-manager__*` 工具是否可用。如不可用，请参考 `/thread` 命令中的错误处理说明。**
