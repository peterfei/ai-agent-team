# ai-agent-backend-dev

后端开发 Agent Skill — API 设计、数据库优化、服务器端逻辑、认证授权、性能优化。

跨 50+ 运行时兼容（Claude Code / Cursor / Codex CLI / Gemini CLI / Windsurf）。

## 安装

```bash
skills install peterfei/ai-agent-backend-dev
```

或手动将 `SKILL.md` 中的运行时配置片段复制到对应工具。

## 能力

- API 设计（RESTful / GraphQL）
- 数据库设计与优化
- 认证与授权（JWT / OAuth / RBAC）
- 错误处理与可观测性

> Agent Skills 开放协议
